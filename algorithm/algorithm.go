package algorithm

import (
	//. ".././driver"
	//. ".././network"
	. "fmt"

//. "strconv"
//"time"
)

//Dette må hentes fra TCP på master sida
func Algorithm(int_order, ext_order, last_order chan string) {
	//Psudo code

}
